const Q={brent:"BZ=F",gold:"GC=F",silver:"SI=F",platinum:"PL=F"};
const endpoint=s=>`https://query1.finance.yahoo.com/v7/finance/quote?symbols=${encodeURIComponent(s.join(","))}`;
const money=(v,d=2)=>v==null?"—":Number(v).toLocaleString(undefined,{minimumFractionDigits:d,maximumFractionDigits:d});
function setChange(el,c,p){if(c==null||p==null){el.textContent="—";el.className="change flat";return}const s=c>0?"+":"";el.textContent=`${s}${money(c)} (${s}${money(p)}%)`;el.className="change "+(c>0?"up":c<0?"down":"flat")}
function paint(prefix,q){document.getElementById(prefix+"Price").textContent=q?.regularMarketPrice==null?"—":"$"+money(q.regularMarketPrice,prefix==="silver"?3:2);setChange(document.getElementById(prefix+"Change"),q?.regularMarketChange,q?.regularMarketChangePercent)}
async function refreshMarkets(){const t=document.getElementById("statusText"),u=document.getElementById("lastUpdated"),dot=document.querySelector(".status-dot");t.textContent="Refreshing markets…";dot.style.background="#fde68a";try{const r=await fetch(endpoint(Object.values(Q)),{cache:"no-store"});if(!r.ok)throw new Error(r.status);const j=await r.json(),m=Object.fromEntries((j.quoteResponse?.result||[]).map(x=>[x.symbol,x]));for(const [k,s] of Object.entries(Q))paint(k,m[s]);t.textContent="Market data loaded";u.textContent="Updated "+new Date().toLocaleTimeString([],{hour:"numeric",minute:"2-digit"});dot.style.background="#86efac"}catch(e){console.error(e);t.textContent="Market feed unavailable";u.textContent="Try Refresh";dot.style.background="#fca5a5"}}
function refreshNHC(){document.getElementById("nhcMap").src="https://www.nhc.noaa.gov/xgtwo/two_atl_7d0.png?t="+Date.now()}
function clock(){document.getElementById("clock").textContent=new Date().toLocaleString(undefined,{weekday:"short",month:"short",day:"numeric",hour:"numeric",minute:"2-digit"})}
document.getElementById("refreshBtn").onclick=refreshMarkets;refreshMarkets();refreshNHC();clock();setInterval(refreshMarkets,300000);setInterval(refreshNHC,900000);setInterval(clock,60000);